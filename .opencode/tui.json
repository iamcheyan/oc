{
  "$schema": "https://opencode.ai/tui.json",
  // The local smoke/vim TUI plugin is kept under .opencode/plugins/ for
  // plugin-runtime testing, but it conflicts with the current opencode-vim
  // mode. Do not declare it here unless testing plugins explicitly.
  "plugin": []
}
