
• 不是，当前还没有做到“所有 minimal 实现都只在 packages/opencode-vim 里”。

  现在这套 minimal 还依赖几处 upstream seam，我确实改过 packages/opencode/src/**。主要有这些：

  packages/opencode/src/cli/cmd/tui/app.tsx:74
  加了一个很薄的 root view 注入点，让 opencode-vim 可以替换 Home/Session。

  packages/opencode/src/cli/cmd/tui/context/minimal.ts:1
  这是我新增的 minimal 环境桥，用来读 OPENCODE_MINIMAL_*。

  packages/opencode/src/cli/cmd/tui/context/theme.tsx:324
  minimal 模式下主题默认值会走这层桥。

  packages/opencode/src/cli/cmd/tui/routes/session/index.tsx:219
  这里有 minimal 相关的 sidebar / animation / scrollbar / spacing 分支。

  packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx:1478
  这里有 minimal prompt 外观分支。

  packages/opencode-vim/ 里当然也有大量实现，但还不是完全自洽、零 upstream 改动的状态。

  所以如果你的目标是“尽量不碰 upstream，减少 merge 冲突”，答案是：

  - 目前还没完全做到
  - 现在是“minimal package + 少量 upstream seam”
  - 还不是“只改 packages/opencode-vim”

  如果你愿意，我下一步可以专门帮你做一次梳理：

  1. 列出现在所有 upstream 改动
  2. 标出哪些必须保留
  3. 哪些可以继续往 packages/opencode-vim 迁移
  4. 尽量把冲突面再缩小一轮
